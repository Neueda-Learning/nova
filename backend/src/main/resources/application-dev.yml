spring:
  datasource:
    url: jdbc:mysql://localhost:3307/portfolio_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: nova_member
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
